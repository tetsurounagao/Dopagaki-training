# CLAUDE.md

このリポジトリで作業するときの不変の前提。毎セッション自動で読まれる。
詳細な計画は [実装計画.md](実装計画.md) と [デプロイ・実機検証計画.md](デプロイ・実機検証計画.md)。

## プロダクト

筋トレアプリ「Dopagaki（ドパガキ）」。スマホ内カメラで筋トレ動作をリアルタイム認識し、
「どの動作を」「何回やったか」に応じてパチスロ風の派手な演出を出す。辛い筋トレをドーパミンで乗り切らせる。
がんばった量を **ポイント** に変換し、後で「体験」（アイテム購入・敵/壁への攻撃など）に接続する構想（詳細未定）。

## ゴール

- **最終目標は一般公開**（Web / PWA。アプリストア配信はしない）。
- まずは **作者本人が実際のトレで使える個人ツール** として完成させ、それを公開版のモックアップとする。
- Phase 5 の堅牢化は「他人の端末・他人の部屋での初回体験」まで見据える。

## 確定事項（変更するときは必ずユーザーに確認）

### プロダクト / スコープ

- **Web アプリ（PWA）**。iOS / Android 両対応。ネイティブ化はしない。
- **バックエンドなし**。データは端末内のみ（IndexedDB / Dexie）。ログイン・同期・ランキングなし。
- **アナリティクス・クラッシュ収集は入れない**（確定）。映像は端末内処理のみ・非送信・非保存。
- **姿勢推定・rep 判定はすべてブラウザ内**（JS/TS + WASM）。サーバー推論しない。Python は実行時に使わない（開発時のオフライン分析のみ任意）。
- **対象種目は立位・正面の4種目に固定** + 「その他（タップカウントのみ・CVなし）」1つ:
  アームカール / ショルダープレス / スクワット / サイドレイズ / その他。将来的に多種目対応したい（Phase 4 後に汎用化を評価）。
- **自動カウント主体 + 手動タップfallback**。タップだけでも全機能が成立する設計。

### 数値・メカニクス

- **総重量 = Σ(rep数 × 実効重量kg)**。フィーバー倍率は影響しない（純粋な仕事量）。
- **ポイント = Σ(rep数 × 実効重量kg × その rep のフィーバー倍率)**。通常 1x / フィーバー中 2x。総重量とは別物としてリザルトに両方出す。
- **実効重量**: 加重種目は登録重量。自重種目（スクワット等）は **登録体重**を実効重量として使う。重量は **kg 固定・0.5kg 刻み・下限 0**。
- **報酬メカニクス**: 各 rep でフィーバー抽選あり。rep ごとにゲージが溜まり、満タン（または抽選当選）で **フィーバー発動**（倍率 2x + GOD 系の重厚な専用演出）。確率・倍率カーブ・ゲージ設計の具体数値は Phase 1 でプレイしながらチューニング。
- **終盤演出**（稲妻・画面フラッシュ）は **最終セットのみ**。フィーバー演出は全セットで起こりうる。
- **目標 rep 到達で自動終了しない**。超過分は「BONUS +N」表示で歓迎。一定時間 動作なしで自動的にセット完了 → 次へ。
- **CV ロスト時**: 静かに推定継続 + 警告アイコン。セット完了画面で **+/- 補正 UI を必ず出す**（`countedBy: 'corrected'`）。
- **左右別カウントは扱わない**。両側同時 or 片側のみ（片側は「よく見えている側」を自動選択）。
- **rep 音声カウント**を入れる（「1！2！」）。音声アセットはユーザーが調達。
- **キャリブレーション**: 種目ごと・端末ごとに初回のみ 3 秒程度の可動域学習を行い、閾値を個人化する。`RepDetector` に最初から組み込む。

### 演出・アセット

- **ノーススター**: パチスロ「GOD」系の演出。重厚感のあるフォント、重量感のある動き、シンプルかつ射幸心を煽る。
- 演出アセットは **商用利用フリー**のもののみ（Lottie / スプライト / 効果音）。ユーザーも素材を探す。当面は仮アセットで作り、後で差し替え。
- カメラ表示は **ミラー**（自撮り感）。detector 内で x 反転を正規化する。

## 技術スタック

- React + TypeScript + Vite / vite-plugin-pwa
- 姿勢推定: MediaPipe Tasks Vision `PoseLandmarker`（Web Worker + OffscreenCanvas）
- 演出: PixiJS v8 + Lottie / 音: Web Audio API（Howler.js）
- 永続化: Dexie（IndexedDB）
- 状態管理: 軽量ステートマシン（XState もしくは自前 reducer）
- CI: GitHub Actions
- ホスティング: Cloudflare Pages（or Vercel）。実機デバッグは Cloudflare Tunnel + `vite dev`

## 進め方のルール

- **1 セッション = 1 フェーズ**。またいで一気にやらない。
- 並列サブエージェントに渡す前に、型・インターフェース（`pose/types.ts` / `RepDetector` 基底 / effects の関数シグネチャ / `game/` の I/F / Dexie スキーマ）を**コードで確定してコミット**する。
- 統合とレビューは必ず本体（親）で行う。
- detector は実機ではなく、記録した landmark 列のリプレイでテストする（`src/detectors/__tests__/`）。フィクスチャ用クリップの仕様は [デプロイ・実機検証計画.md](デプロイ・実機検証計画.md) §9.3.1。
- 演出まわりの変更は起動してスクリーンショットで確認する。
- フェーズ着手時は使い捨ての作業メモ（`docs/phase-N.md` など）を作り、変動はそこに書く。マスター計画は安定させる。
- 進行中セッションは IndexedDB に逐次保存し、起動時に「続きから」を出す。

## コマンド（実装開始後に追記する）

- dev: `npm run dev`
- build: `npm run build`
- test: `npm test`
- typecheck: `npm run typecheck`
- 実機デバッグ: `cloudflared tunnel --url http://localhost:5173`
