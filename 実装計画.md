# 筋トレアプリ 実装計画

最終更新: 2026-09-03

---

## 0. 関連ドキュメント

| ファイル | 内容 | いつ読むか |
|---|---|---|
| [デプロイ・実機検証計画.md](デプロイ・実機検証計画.md) | デプロイ環境（Tunnel / Cloudflare Pages）、実機マトリクス、挙動検証（fps・発熱・detector 精度）の測り方、デバッグ HUD、チェックリスト、トラブルシューティング | Phase 0（デプロイ基盤）、Phase 2〜3（実機計測）、Phase 5（PWA 検証） |

> 分量が嵩む詳細は上記の別ファイルに切り出している。本体には各フェーズから「→ 詳細は〜」でリンクする。

---

## 1. コンセプト

普通ならきつい筋トレを、スマホ内カメラで動作をリアルタイム認識し、「どんな動作を」「何回やったか」に基づいてパチスロ風の派手な演出を出すことで、ドーパミンで乗り切らせる Web アプリ。

---

## 2. 確定した前提（2026-09-03 時点）

| 項目 | 決定 | 備考 |
|---|---|---|
| 対象種目 | 立位・正面のみ、4種目に固定 | 汎用性が見えたら追加を検討（Phase 4 後に評価） |
| 初期4種目 | アームカール / ショルダープレス / スクワット / サイドレイズ | いずれも正面固定カメラで画面が見える |
| 自動カウント | 主体。ただし検出不能時は手動タップにフォールバック | タップだけでも全機能が成立するよう設計 |
| 配布形態 | PWA（インストール可）、iOS/Android 両対応 | ストア審査なし・即時更新。ネイティブ化は将来検討 |
| データ | 端末内のみ（IndexedDB）。ログイン・同期・ランキングなし | バックエンドは Phase 対象外 |
| 演出アセット | 一旦は既製品（Lottie / スプライト / フリー効果音）を流用 | Phase 1 デモを見て参照とすり合わせ、後で差し替え |
| 重量単位 | kg 固定 | lb・チューブは対象外 |
| 左右別カウント | 扱わない | ダンベル左右交互などは対象外。両側同時 or 片側のみ |

---

## 3. 技術選定

| 領域 | 採用 | 理由 / 代替案 |
|---|---|---|
| フレームワーク | React + TypeScript + Vite | 実績・エコシステム。代替: Svelte（ランタイム軽量） |
| PWA | vite-plugin-pwa (Workbox) | manifest / Service Worker / プリキャッシュ |
| 姿勢推定 | MediaPipe Tasks Vision `PoseLandmarker` | 33点3Dランドマーク、WASM+GPU、モバイル最適化。Web Worker + OffscreenCanvas で別スレッド化。代替: TF.js MoveNet（2D 17点・軽量） |
| 演出・描画 | PixiJS v8（ゲージ・数字・パーティクル） + Lottie（稲妻・賞賛） | パチスロ感の 2D 表現に最適。3D 不要なので Three.js は使わない |
| 音 | Web Audio API（Howler.js ラッパ） | 初回タップで AudioContext を unlock（iOS 対策） |
| 永続化 | IndexedDB（Dexie） | ルーティン定義・実施履歴・総挙上量 |
| 状態管理 | 軽量ステートマシン（XState もしくは自前 reducer） | トレーニングフローの遷移を明示的に |
| スリープ防止 | Screen Wake Lock API | Safari 16.4+ / Chrome。非対応時は無音動画ループでフォールバック |

---

## 4. データモデル（Dexie）

```ts
// 種目マスタ（アプリ同梱、固定4種目）
interface Exercise {
  id: 'armCurl' | 'shoulderPress' | 'squat' | 'sideRaise';
  name: string;              // 表示名
  detector: string;          // RepDetector の種別キー
  mode: 'both' | 'single';   // 両側同時 / 片側のみ
}

// ルーティン（ユーザー作成）
interface Routine {
  id: string;
  name: string;
  createdAt: number;
  menus: Menu[];             // 実施順
}

interface Menu {
  id: string;
  exerciseId: Exercise['id'];
  weightKg: number;
  repsPerSet: number;
  sets: number;
  restSec: number;           // セット間インターバル
}

// 実施セッション（履歴）
interface WorkoutSession {
  id: string;
  routineId: string;
  startedAt: number;
  finishedAt: number | null;
  entries: SessionEntry[];
  totalVolumeKg: number;     // Σ(weightKg × 完了セット数)
}

interface SessionEntry {
  menuId: string;
  exerciseId: Exercise['id'];
  weightKg: number;
  completedSets: CompletedSet[];
}

interface CompletedSet {
  reps: number;
  countedBy: 'auto' | 'manual';
  durationSec: number;
}
```

**総挙上量の計算**: `Σ (weightKg × 完了セット数)`。
例: ダンベル10kg 3セット + 20kg 3セット → 10×3 + 20×3 = **90kg**。
※ 回数（reps）は乗じない。やり切ったセット数のみを重量に乗じる。

---

## 5. アプリフロー（ステートマシン）

```
[ホーム/ルーティン一覧]
      │ ルーティン選択 → 開始
      ▼
[カメラ承認]  ── 拒否 → [手動モード案内] ─┐
      │ 許可                              │
      ▼                                   │
[種目セットアップ]  ← カメラ位置ガイド表示 │
      │ 準備OK                            │
      ▼                                   │
[カウントダウン 3..2..1]                  │
      ▼                                   ▼
[セット中] ─ rep ごとに +1 / ゲージ充填 / 数字ポップ
      │  終盤(残り5〜10回)で演出強度アップ（稲妻・画面フラッシュ）
      │  目標 reps 到達
      ▼
[セット完了演出]  派手な賞賛
      ▼
[インターバル]  restSec カウントダウン
      ▼
   ┌──「もう1セットやる」→ 暗転 → フラッシュ → 「もう1セット突入」大型テキスト → [カウントダウン]
   └──「次のメニューへ」→ 次 Menu があれば [種目セットアップ] / なければ ▼
      ▼
[リザルト]  総挙上量 + 種目別内訳 + 履歴保存
```

---

## 6. ディレクトリ構成（案）

```
src/
  main.tsx
  app/
    AppFlow.tsx            # ルート・トップレベル状態機械
    machine.ts             # 遷移定義
  features/
    routine/               # ルーティン一覧・登録・編集
    workout/               # 実行画面（セット中・インターバル・分岐）
    result/                # リザルト
    history/               # 履歴一覧（Phase 5）
  camera/
    useCamera.ts           # getUserMedia / デバイス選択 / 権限
  pose/
    poseWorker.ts          # MediaPipe を動かす Web Worker
    PoseClient.ts          # メインスレッド側ラッパ（型付き I/F）
    types.ts               # Landmark[] などの型
  detectors/
    RepDetector.ts         # 基底クラス／インターフェース
    angles.ts              # 3点角度・EMA平滑化・信頼度ゲート
    armCurl.ts
    shoulderPress.ts
    squat.ts
    sideRaise.ts
    __tests__/             # 記録した landmark 列のリプレイテスト
  effects/
    Stage.ts               # PixiJS ステージ管理
    gauge.ts               # エネルギーゲージ
    repNumber.ts           # 数字ポップ
    lightning.ts           # 稲妻（Lottie 差し込み）
    praise.ts              # 賞賛バースト
    transition.ts          # 「もう1セット突入」暗転→フラッシュ
    sound.ts               # 効果音・AudioContext unlock
  store/
    db.ts                  # Dexie スキーマ
    routines.ts
    sessions.ts
  ui/                      # 共通コンポーネント（ボタン・数値入力など）
public/
  models/                  # MediaPipe .task ファイル
  effects/                 # Lottie JSON / スプライト / 音源
```

---

## 7. RepDetector 設計

### 共通インターフェース

```ts
interface RepDetectorResult {
  repCompleted: boolean;         // このフレームで1回完了したか
  phase: 'idle' | 'down' | 'up'; // 現在の局面
  confidence: number;            // 0..1（低い状態が続くとタップUIへ）
  rangeOk: boolean;              // 可動域が十分か（浅い動作の警告用）
}

abstract class RepDetector {
  abstract update(landmarks: Landmark[], tMs: number): RepDetectorResult;
  reset(): void;
}
```

### 共通ロジック

- 対象関節角度 `angle(t)` を算出 → EMA で平滑化。
- **2値ヒステリシス状態機械**: `down 閾値` を下回ったら `down`、`up 閾値` を上回ったら `up`。`down → up → down`（もしくは種目に応じた1往復）で 1 rep。
- **ゲート条件**（誤検出抑制）:
  - 関連ランドマークの可視性スコアが閾値以上。
  - 1 rep の所要時間が下限以上（連打・ブレ除外）。
  - 可動域（角度振幅）が下限以上 → 満たさない rep は `rangeOk=false` で「浅い」警告のみ、カウントは方針次第。
- **フォールバック**: `confidence` が一定フレーム連続で低い → 実行画面が自動でタップUIを前面に出す。

### 種目別パラメータ

| 種目 | 主要角度 | 補助チェック | 1 rep の定義 |
|---|---|---|---|
| アームカール | 肩-肘-手首 | 肩-肘ベクトルが概ね固定（上腕を振らない） | 肘伸展 → 屈曲 → 伸展 |
| ショルダープレス | 肩-肘-手首 + 手首の縦位置 | 手首が肩ラインより上に抜ける | 肘屈曲(肩の高さ) → 伸展(頭上) → 屈曲 |
| スクワット | 股-膝-足首 + 股関節の下降量 | 骨盤が一定量下がる | 立位 → 屈曲 → 立位 |
| サイドレイズ | 体幹-肩-肘（肩の外転角） | 肘がほぼ伸展維持 | 下制 → 外転(水平付近) → 下制 |

---

## 8. フェーズ別マイルストーン

> 各フェーズ冒頭で **型・インターフェースを先に確定** してからサブエージェントに分担を渡す。

### Phase 0: プロジェクト基盤（小）

- Vite + React + TS scaffold、vite-plugin-pwa、ESLint / Prettier、Vitest。
- ルーティング、3画面の骨組み（ルーティン一覧 → 実行 → リザルト）。
- Dexie スキーマ定義（セクション4）。
- **デプロイ基盤**: Cloudflare Pages（or Vercel）にリポジトリ連携。ビルド設定・SPA フォールバック・キャッシュヘッダ・プレビュー URL 発行まで。→ 詳細は [デプロイ・実機検証計画.md](デプロイ・実機検証計画.md) の §5・§6・§12・§13。
- **サブエージェント**: 不要（本体で実施）。あるいは scaffold のみ 1 エージェント。
- **受け入れ**: `npm run dev` で3画面遷移。`main` push で本番 URL、ブランチ push でプレビュー URL が出る。実機1台でホーム画面追加まで確認。

### Phase 1: タップカウンタ版で「ドーパミンループ」を完成（最重要）

CV なしで、体験の芯（登録 → 実行 → 演出 → 分岐 → リザルト）を作り切る。

- ルーティン登録UI（種目・kg・reps/set・sets・restSec の CRUD、Dexie 保存）。
- 実行画面のステートマシン（セクション5）と画面遷移。
- セット中: 大きなタップ領域で rep +1、ゲージ充填、数字ポップ。
- 終盤演出: 残り 5〜10 回で稲妻・画面フラッシュ・音の強化。
- セット完了演出 → インターバル（restSec カウントダウン）。
- 分岐: 「もう1セットやる」= 暗転 → フラッシュ → 「もう1セット突入」大型テキスト → カウントダウンへ。「次のメニューへ」= 次 Menu or リザルト。
- リザルト画面: 総挙上量 + 種目別内訳、履歴を Dexie に保存。
- effects/ 一式（PixiJS ステージ、ゲージ、数字、稲妻 Lottie、賞賛 Lottie、sound、transition）。
- **サブエージェント分担（並列）**:
  - A: ルーティン登録UI + Dexie CRUD（`store/`, `features/routine/`）
  - B: 実行画面ステートマシン + 画面遷移（`app/machine.ts`, `features/workout/`）※演出は stub 呼び出し
  - C: `effects/` 一式（PixiJS + Lottie + sound、B が呼ぶ関数シグネチャに合わせる）
  - 統合と最終結線は本体。
- **受け入れ**: カメラ無しで1ルーティンを最初から最後まで通せる。終盤で演出が強化される。リザルトの数字が正しい。リロードしても履歴が残る。
- **この時点で参照（実機・動画）とすり合わせ**、演出の方向性を確定 → アセット差し替え計画。

### Phase 2: 姿勢推定の導入

- MediaPipe `PoseLandmarker` を Web Worker + OffscreenCanvas で起動。
- `<video>` フレームを Worker へ転送、33点ランドマークを受信する型付き I/F（`pose/PoseClient.ts`）。
- 実行画面にスケルトン重畳（デバッグトグル）。
- 実機計測: iOS Safari / Android Chrome で fps・発熱・電池消費。**Cloudflare Tunnel + `vite dev`** で HMR を効かせながら実機デバッグ。デバッグ HUD（fps / 推論ms / visibility / phase / セッション記録）を実装。→ 手順・指標・目標値は [デプロイ・実機検証計画.md](デプロイ・実機検証計画.md) の §3.2・§9.1・§9.2・§10。
- スロットリング: 派手な演出中は推論 fps を落とす。Page Visibility で停止。
- **サブエージェント**: 「MediaPipe Tasks Vision の Worker + OffscreenCanvas セットアップ最新手順」を調査系エージェント（Explore / general-purpose）に投げ、結論だけ回収。実装は本体（中核なので直列）。
- **受け入れ**: 実機で 15fps 以上を維持しつつ演出が破綻しない。バックグラウンドで推論停止・復帰。

### Phase 3: RepDetector（アームカール先行）

- `detectors/angles.ts`（3点角度、EMA、信頼度ゲート）。
- `RepDetector` 基底 + `armCurl.ts` 実装（セクション7）。
- 実行画面と接続: 自動 rep で演出発火、`confidence` 低下継続でタップUIへ自動フォールバック。
- 実負荷テスト: 実際に汗をかいて誤検出率・取りこぼし率を条件別に記録（テンポ、距離、明るさ、服のコントラスト）。HUD の記録機能で landmark 列を JSON ダンプし `__tests__/` のリプレイ資産にする。→ 計測条件・定義・手順は [デプロイ・実機検証計画.md](デプロイ・実機検証計画.md) の §9.3。
- **サブエージェント**: なし（判定品質の肌感が要るため本体で作り込む）。
- **受け入れ**: 通常テンポのカールで誤検出 < 5% / 取りこぼし < 10%（目標値、実測して閾値調整）。

### Phase 4: 残り3種目へ横展開

- `shoulderPress` / `squat` / `sideRaise` を同じ基底で実装。
- カメラ位置ガイド オーバーレイ（全身が枠に収まっているか、明るさ判定）。
- **サブエージェント分担（並列）**: 3種目を 3 エージェントに。各自 detector 実装 + リプレイ単体テスト（実機ではなく Phase 3 で収集したリプレイ資産を使う）。`RepDetector` 基底と `angles.ts` は Phase 3 で確定済みなので独立して進む。
- 精度計測は Phase 3 と同じプロトコル（[デプロイ・実機検証計画.md](デプロイ・実機検証計画.md) §9.3）を各種目に適用。
- **受け入れ**: 4種目すべてで、自動 + タップfallback 込みで1ルーティン完走。
- **ここで detector の汎用性を評価** → 種目追加の可否・追加コストを判断（確認事項 #4）。

### Phase 5: PWA 仕上げ・堅牢化

- Service Worker でモデル / アセットをプリキャッシュ（機内モードで起動可能に）。
- Wake Lock、画面回転ロック、カメラ権限拒否・切断・剥奪からのリカバリUI。
- 履歴一覧画面、過去リザルトの振り返り。
- エラーバウンダリ、AudioContext unlock、iOS standalone PWA でのカメラ動作確認。
- **サブエージェント**: 履歴一覧画面を 1 エージェントに切り出し可能。
- PWA 検証項目・実機マトリクスでのサインオフは [デプロイ・実機検証計画.md](デプロイ・実機検証計画.md) の §7・§8・§13 に従う。
- **受け入れ**: 機内モードで起動〜完走。権限拒否から復帰できる。実機2 OS で通し確認。

---

## 9. サブエージェント運用方針

- **並列化できる**: 独立したファイル群を新規に作るタスク。
  - Phase 1: 登録UI / ステートマシン / effects（3並列）
  - Phase 4: detector 3種目（3並列）
  - Phase 5: 履歴一覧画面
- **直列でやる（本体）**: ステートマシン中核、Worker 統合、実行画面の結線、判定品質の作り込み（Phase 3）。
- **調査系はエージェントに委譲**: MediaPipe の最新セットアップ手順、iOS Safari の getUserMedia/PWA 挙動など。結論とコード断片だけ回収する。
- **渡す前に必ず I/F を固める**: 型定義（`pose/types.ts`、`RepDetector`、effects の関数シグネチャ、Dexie スキーマ）を先にコミットしてから分担を投げる。エージェントには「このファイルだけ触る / この型に従う / テストも書く」を明示。
- **統合は必ず本体**: 各エージェントの成果物を本体でレビューして結線する。

---

## 10. 横断的な非機能要件

- **発熱・電池**: 推論解像度を下げる / 演出中スロットル / Visibility で停止 / セッション時間の目安表示。
- **iOS Safari 固有**: standalone PWA でのカメラ、初回タップで AudioContext resume、Wake Lock 非対応時のフォールバック。
- **プライバシー**: 映像は端末内でのみ処理し外部送信しない旨を UI に明記。録画・保存もしない。
- **アクセシビリティ**: 音 OFF でも成立するよう視覚フィードバックを担保。タップ領域を大きく。

---

## 11. テスト戦略

- **detector ユニットテスト**: 実機で記録した landmark 時系列を JSON 化し、リプレイして rep 数・phase 遷移を検証。カメラ不要、CI で回す。
- **ステートマシン**: 遷移テーブルのトランジションテスト（「もう1セット」「次へ」「最終セット」分岐）。
- **手動 E2E チェックリスト**: 実機カメラでの通し確認（2 OS）。権限拒否・切断・機内モード。詳細な手順・端末マトリクス・挙動検証（fps・発熱・detector 精度）は [デプロイ・実機検証計画.md](デプロイ・実機検証計画.md) を参照。

---

## 12. 確認事項（バックログ）

1. ~~リザルト総挙上量の定義~~ **決定済み (2026-09-03)**: `Σ(重量 × 完了セット数)`。回数は乗じない。例: 10kg×3 + 20kg×3 = 90kg。
2. **演出の参照**: Phase 1 デモ後に実機・動画の参照とすり合わせ、既製アセットを差し替え。
3. **rep 判定の厳しさ**: モチベ優先で「甘め」に倒す方針でよいか（浅い rep もカウントするか、警告に留めるか）。
4. **種目追加**: Phase 4 完了時に detector の汎用性を評価してから判断。
5. **「浅い rep」の扱い**: `rangeOk=false` の rep をカウントに含めるか、演出だけ弱めるか。

---

## 13. 次アクション

- [x] 確認事項 #1（総挙上量の定義）を決める → `Σ(重量 × 完了セット数)`
- [ ] Phase 0 の scaffold に着手
- [ ] Phase 1 の型 I/F（Dexie スキーマ / ステートマシン遷移 / effects シグネチャ）を先に確定
