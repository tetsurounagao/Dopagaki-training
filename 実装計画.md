# 筋トレアプリ 実装計画

最終更新: 2026-09-03（第2回設計判断を反映）

---

## 0. 関連ドキュメント

| ファイル | 内容 | いつ読むか |
|---|---|---|
| [CLAUDE.md](CLAUDE.md) | 不変の前提・確定事項・進め方ルール | 毎回（自動ロード） |
| [デプロイ・実機検証計画.md](デプロイ・実機検証計画.md) | デプロイ環境（Tunnel / Cloudflare Pages）、実機マトリクス、挙動検証（fps・発熱・detector 精度）、フィクスチャ用クリップ仕様、デバッグ HUD、チェックリスト | Phase 0（デプロイ基盤）、Phase 2〜3（実機計測・クリップ収集）、Phase 5（PWA 検証） |

> 分量が嵩む詳細は別ファイルに切り出している。本体には各フェーズから「→ 詳細は〜」でリンクする。

---

## 1. コンセプト

普通ならきつい筋トレを、スマホ内カメラで動作をリアルタイム認識し、「どんな動作を」「何回やったか」に基づいてパチスロ風の派手な演出を出すことで、ドーパミンで乗り切らせる Web アプリ。

やりきった量を **ポイント**（フィーバー倍率で変動する得点）に変換し、後々そのポイントを「体験」に接続する構想 — アイテム購入、「敵」や「壁」への攻撃 → 撃破で特典 → より強い敵、など。詳細は未定だが、**がんばった結果を体験に変える**方向。

---

## 2. 確定した前提

### プロダクト / スコープ

| 項目 | 決定 |
|---|---|
| 配布形態 | PWA（インストール可）、iOS/Android 両対応。ストア配信はしない |
| ゴール | 最終目標は一般公開。まず作者本人の実トレ用ツールとして完成させ、公開版のモックとする |
| データ | 端末内のみ（IndexedDB / Dexie）。ログイン・同期・ランキングなし |
| アナリティクス | 入れない（確定）。映像は端末内処理のみ・非送信・非保存 |
| 姿勢推定・rep 判定 | すべてブラウザ内（JS/TS + WASM）。サーバー推論しない |
| 対象種目 | 立位・正面の4種目に固定 + 「その他（タップのみ・CVなし）」1つ。アームカール / ショルダープレス / スクワット / サイドレイズ / その他 |
| 種目追加 | 将来的に多種目対応したい。Phase 4 後に detector 汎用化を評価 |
| カウント方式 | 自動主体 + 手動タップfallback。タップだけでも全機能成立 |

### 数値・メカニクス

| 項目 | 決定 |
|---|---|
| 総重量 | `Σ(rep数 × 実効重量kg)`。フィーバー倍率の影響を受けない（純粋な仕事量） |
| ポイント | `Σ(rep数 × 実効重量kg × その rep のフィーバー倍率)`。通常 1x / フィーバー中 2x。リザルトに総重量と両方表示 |
| 実効重量 | 加重種目＝登録重量。自重種目（スクワット等）＝登録体重。kg 固定・0.5kg 刻み・下限 0 |
| フィーバー | 各 rep で抽選。rep ごとにゲージが溜まり、満タン or 抽選当選で発動（倍率 2x + GOD 系演出）。確率・倍率・ゲージ設計の数値は Phase 1 でチューニング |
| 終盤演出 | 稲妻・画面フラッシュは**最終セットのみ**。フィーバー演出は全セットで起こりうる |
| 目標 rep 到達 | 自動終了しない。超過は「BONUS +N」表示。一定時間 動作なしで自動的にセット完了 → 次へ |
| CV ロスト | 推定継続 + 警告アイコン。セット完了画面で +/- 補正 UI 必須（`countedBy: 'corrected'`） |
| 左右別カウント | 扱わない。両側同時 or 片側のみ（片側は「よく見えている側」を自動選択） |
| 音声カウント | 入れる（「1！2！」）。音声アセットはユーザー調達 |
| キャリブレーション | 種目・端末ごとに初回のみ 3 秒程度の可動域学習。`RepDetector` に最初から組み込む |

### 演出・アセット

| 項目 | 決定 |
|---|---|
| ノーススター | パチスロ「GOD」系。重厚なフォント、重量感のある動き、シンプルかつ射幸心 |
| アセット | 商用利用フリーのみ（Lottie / スプライト / 効果音）。当面は仮アセット、後で差し替え。ユーザーも素材を探す |
| カメラ表示 | ミラー（自撮り感）。detector 内で x 反転を正規化 |

---

## 3. 技術選定

| 領域 | 採用 | 理由 / 代替案 |
|---|---|---|
| フレームワーク | React + TypeScript + Vite | 実績・エコシステム。代替: Svelte |
| PWA | vite-plugin-pwa (Workbox) | manifest / Service Worker / プリキャッシュ |
| 姿勢推定 | MediaPipe Tasks Vision `PoseLandmarker` | 33点3Dランドマーク、WASM+GPU、モバイル最適化。Web Worker + OffscreenCanvas で別スレッド化。代替: TF.js MoveNet |
| 演出・描画 | PixiJS v8 + Lottie | パチスロ感の 2D 表現に最適。3D 不要 |
| 音 | Web Audio API（Howler.js） | 初回タップで AudioContext unlock。rep 音声カウントもここ |
| 永続化 | IndexedDB（Dexie） | ルーティン / 履歴 / 設定 / キャリブレーション / 進行中セッション |
| 状態管理 | 軽量ステートマシン（XState もしくは自前 reducer） | トレーニングフローの遷移を明示的に |
| CI | GitHub Actions | typecheck / lint / detector リプレイテスト |
| スリープ防止 | Screen Wake Lock API | Safari 16.4+ / Chrome。非対応時は無音動画ループ |

---

## 4. データモデル（Dexie）

```ts
// ---- アプリ同梱マスタ ----
interface Exercise {
  id: 'armCurl' | 'shoulderPress' | 'squat' | 'sideRaise' | 'other';
  name: string;
  detector: 'armCurl' | 'shoulderPress' | 'squat' | 'sideRaise' | 'none'; // 'none' = タップのみ
  mode: 'both' | 'single';
  bodyweightBased: boolean;   // true かつ weightKg=0 のとき登録体重を実効重量に使う
}

// ---- ユーザー設定（単一レコード id='singleton'）----
interface UserSettings {
  id: 'singleton';
  bodyWeightKg: number;       // 自重種目の実効重量
  soundEnabled: boolean;
  voiceCountEnabled: boolean;
}

// ---- キャリブレーション（種目ごと・端末ごとに1件）----
interface CalibrationProfile {
  exerciseId: Exercise['id'];
  params: Record<string, number>;  // detector が学習した値（可動域の上端/下端角度など。実装依存）
  updatedAt: number;
}

// ---- ルーティン（ユーザー作成）----
interface Routine {
  id: string;
  name: string;
  createdAt: number;
  menus: Menu[];             // 実施順
}

interface Menu {
  id: string;
  exerciseId: Exercise['id'];
  weightKg: number;          // 0 かつ bodyweightBased なら UserSettings.bodyWeightKg を使用
  repsPerSet: number;
  sets: number;
  restSec: number;
}

// ---- 実施セッション（履歴 & 進行中）----
interface WorkoutSession {
  id: string;
  routineId: string;
  routineSnapshot: Routine;  // 実施時点のルーティン内容を丸ごとスナップショット
  startedAt: number;
  finishedAt: number | null; // null = 進行中（復帰対象。常に高々1件）
  progress: SessionProgress; // 中断復帰用の現在地
  entries: SessionEntry[];
  totalVolumeKg: number;     // Σ(reps × effectiveWeightKg)
  totalPoints: number;       // Σ(reps × effectiveWeightKg × フィーバー倍率)
}

interface SessionProgress {
  menuIndex: number;
  setIndex: number;
  repInCurrentSet: number;
  phase: string;             // ステートマシンの状態名
}

interface SessionEntry {
  menuId: string;
  exerciseId: Exercise['id'];
  effectiveWeightKg: number; // 実際に計上した重量（自重なら体重をスナップショット）
  completedSets: CompletedSet[];
}

interface CompletedSet {
  reps: number;
  feverReps: number;         // うちフィーバー中だった rep 数
  points: number;            // このセットで得たポイント（倍率適用済み）
  countedBy: 'auto' | 'manual' | 'corrected';
  durationSec: number;
}
```

### 計算の定義

- **実効重量** `effectiveWeightKg` = `menu.weightKg > 0 ? menu.weightKg : (exercise.bodyweightBased ? settings.bodyWeightKg : 0)`
- **総重量** `totalVolumeKg` = Σ 全 `CompletedSet` について `reps × effectiveWeightKg`
- **ポイント** `totalPoints` = Σ 全 rep について `effectiveWeightKg × その rep の倍率`
  実装上は `Σ CompletedSet: effectiveWeightKg × ((reps - feverReps) × 1 + feverReps × 2)`
- フィーバーが一度も出なければ `totalPoints === totalVolumeKg`。
- 例: 10kg×10rep×3set + 20kg×10rep×3set、フィーバーなし → 総重量 = ポイント = 900。

---

## 5. アプリフロー（ステートマシン）

```
[ホーム/ルーティン一覧]
   │ ├─ 進行中セッションあり → [続きから？] ── はい → 中断地点へ復帰
   │ └─ ルーティン選択 → 開始
   ▼
[カメラ承認]  ── 拒否 → [手動モード] （以降カメラ工程をスキップして手動タップで進行）
   │ 許可
   ▼
[種目セットアップ]  ← カメラ位置ガイド（全身が枠内か / 明るさ）
   │ この種目が未キャリブレーション → [キャリブレーション]（3秒静止 + ゆっくり1rep）→ プロファイル保存
   │ 準備OK
   ▼
[カウントダウン 3..2..1]
   ▼
[セット中]
   ├ rep ごとに: カウント+1 / 音声「N！」 / ゲージ充填 / 数字ポップ / フィーバー抽選
   ├ フィーバー発動時: 倍率2x + GOD系専用演出（全セットで起こりうる）
   ├ 最終セットのみ: 終盤(残り5〜10 rep)で稲妻・画面フラッシュ
   ├ 目標 rep 到達: 自動終了せず「BONUS +N」表示で超過を歓迎
   ├ CV ロスト: 推定継続 + 警告アイコン
   │ 一定時間 動作なし
   ▼
[セット完了]
   ├ CV ロストがあった場合: +/- 補正 UI
   ├ 派手な賞賛演出
   ▼
[インターバル]  restSec カウントダウン（スキップ / +30秒）
   ▼
 ┌─「もう1セットやる」→ 暗転 → フラッシュ → 「もう1セット突入」大型テキスト → [カウントダウン]
 └─「次のメニューへ」→ 次 Menu があれば [種目セットアップ] / なければ ▼
   ▼
[リザルト]  総重量 + ポイント + 種目別内訳 → セッションを finishedAt 付きで確定保存
```

- **進行中セッション永続化**: `[セット中]` の rep 完了ごと、および各状態遷移ごとに `WorkoutSession.progress` と `entries` を Dexie に書く。`finishedAt` は `[リザルト]` 到達時のみ設定。
- **復帰**: 起動時に `finishedAt === null` のセッションがあれば「続きから」を提示。破棄も選べる。

---

## 6. ディレクトリ構成（案）

```
src/
  main.tsx
  app/
    AppFlow.tsx            # ルート・トップレベル状態機械
    machine.ts             # 遷移定義
  features/
    routine/               # ルーティン一覧・登録・編集
    workout/               # 実行画面（セットアップ・キャリブレーション・セット中・補正・分岐）
    result/                # リザルト（総重量 + ポイント）
    history/               # 履歴一覧（Phase 5）
    onboarding/            # 体重登録など初回設定
  camera/
    useCamera.ts           # getUserMedia / デバイス選択 / 権限 / ミラー
  pose/
    poseWorker.ts          # MediaPipe を動かす Web Worker
    PoseClient.ts          # メインスレッド側ラッパ（型付き I/F）
    types.ts               # Landmark[] などの型
  detectors/
    RepDetector.ts         # 基底（update / calibrateSample / finalizeCalibration）
    angles.ts              # 3点角度・EMA平滑化・信頼度ゲート・x反転正規化
    armCurl.ts / shoulderPress.ts / squat.ts / sideRaise.ts
    __tests__/fixtures/    # 記録した landmark 列（JSON）+ メタ
  game/
    PointsEngine.ts        # rep イベント → ポイント加算（倍率適用）
    feverMachine.ts        # ゲージ・抽選・フィーバー状態
  effects/
    Stage.ts               # PixiJS ステージ管理
    gauge.ts / repNumber.ts / lightning.ts / praise.ts / fever.ts / transition.ts
    sound.ts               # 効果音・音声カウント・AudioContext unlock
  store/
    db.ts                  # Dexie スキーマ
    routines.ts / sessions.ts / settings.ts / calibration.ts
  ui/                      # 共通コンポーネント
public/
  models/                  # MediaPipe .task
  effects/                 # Lottie JSON / スプライト / 音源（仮 → 商用フリーに差し替え）
```

---

## 7. RepDetector 設計

### 共通インターフェース

```ts
interface RepDetectorResult {
  repCompleted: boolean;         // このフレームで1回完了したか
  phase: 'idle' | 'down' | 'up'; // 現在の局面
  confidence: number;            // 0..1（低い状態が続くと警告 → 補正UIへ）
  rangeOk: boolean;              // 可動域が十分か（浅い動作の警告用）
}

abstract class RepDetector {
  constructor(profile?: CalibrationProfile);           // 既存プロファイルがあれば個人化して起動
  abstract update(landmarks: Landmark[], tMs: number): RepDetectorResult;
  // --- キャリブレーション（初回のみ。update の代わりに数秒間これを呼ぶ）---
  abstract calibrateSample(landmarks: Landmark[], tMs: number): void;
  abstract finalizeCalibration(): CalibrationProfile;  // 蓄積したサンプルから params を確定
  reset(): void;
}
```

- フィーバー抽選・ポイント計算は detector の責務ではない。detector は純粋に rep イベントと phase/confidence を返すだけ。`game/` 層が rep イベントを消費する。
- 入力ランドマークは `angles.ts` でミラー（x 反転）を正規化してから角度計算する。片側種目は左右のうち可視性の高い側を選ぶ。

### 共通ロジック

- 対象関節角度 `angle(t)` を算出 → EMA で平滑化。
- **2値ヒステリシス状態機械**: `down 閾値` を下回ったら `down`、`up 閾値` を上回ったら `up`。1往復で 1 rep。
- **閾値はキャリブレーション由来**: 初回学習した可動域の上端/下端から LOW/HIGH を個人ごとに決める。未キャリブレーション時は種目デフォルト値。
- **ゲート条件**: 関連ランドマークの可視性 ≥ 閾値 / 1 rep 所要時間 ≥ 下限 / 可動域振幅 ≥ 下限（満たさない rep は `rangeOk=false`、カウント方針は §12-3）。
- **フォールバック**: `confidence` が一定フレーム連続で低い → 警告アイコン。セット完了画面で補正 UI。

### 種目別パラメータ

| 種目 | 主要角度 | 補助チェック | 1 rep の定義 |
|---|---|---|---|
| アームカール | 肩-肘-手首 | 肩-肘ベクトルが概ね固定 | 肘伸展 → 屈曲 → 伸展 |
| ショルダープレス | 肩-肘-手首 + 手首の縦位置 | 手首が肩ラインより上に抜ける | 肘屈曲(肩の高さ) → 伸展(頭上) → 屈曲 |
| スクワット | 股-膝-足首 + 股関節の下降量 | 骨盤が一定量下がる（体幹長で正規化） | 立位 → 屈曲 → 立位 |
| サイドレイズ | 体幹-肩-肘（肩の外転角） | 肘がほぼ伸展維持 | 下制 → 外転(水平付近) → 下制 |
| その他 | — | — | detector 'none'。タップのみ |

---

## 8. フェーズ別マイルストーン

> 各フェーズ冒頭で **型・インターフェースを先に確定** してからサブエージェントに分担を渡す。

### Phase 0: プロジェクト基盤（小）

- Vite + React + TS scaffold、vite-plugin-pwa、ESLint / Prettier、Vitest、GitHub Actions（typecheck / lint / test）。
- ルーティング、画面の骨組み（ルーティン一覧 → 実行 → リザルト → オンボーディング）。
- Dexie スキーマ定義（§4 全 interface：Exercise マスタ投入、UserSettings、CalibrationProfile、Routine、WorkoutSession）。
- オンボーディング: 体重登録（自重種目の実効重量）。
- **デプロイ基盤**: Cloudflare Pages（or Vercel）連携。ビルド設定・SPA フォールバック・キャッシュヘッダ・プレビュー URL。→ [デプロイ・実機検証計画.md](デプロイ・実機検証計画.md) §5・§6・§12・§13。
- **サブエージェント**: 不要（本体）。
- **受け入れ**: `npm run dev` で画面遷移。`main` push で本番 URL、ブランチ push でプレビュー URL。実機1台でホーム画面追加まで確認。

### Phase 1: タップカウンタ版で「ドーパミンループ」を完成（最重要）

CV なしで、体験の芯（登録 → 実行 → 演出 → フィーバー → 分岐 → リザルト）を作り切る。

- ルーティン登録 UI（種目・kg（0.5刻み）・reps/set・sets・restSec の CRUD、Dexie 保存）。種目は固定4 + その他。
- 実行画面ステートマシン（§5）と画面遷移。キャリブレーション状態は「スキップ可能な空実装」で用意（中身は Phase 3）。
- セット中: 大きなタップ領域で rep +1、音声カウント、ゲージ充填、数字ポップ。
- **`game/` 層**: `PointsEngine`（rep イベント → ポイント加算）+ `feverMachine`（ゲージ・抽選・フィーバー中の倍率2x）。確率・倍率・ゲージ量は設定可能な定数にして仮値で置く。
- 演出: PixiJS ステージ、ゲージ、数字、フィーバー（GOD 系・仮アセット）、最終セット終盤の稲妻・フラッシュ、セット完了の賞賛、「もう1セット突入」トランジション、効果音。
- BONUS 表示（目標超過）、一定時間 無操作で自動セット完了。
- インターバル（restSec カウントダウン、スキップ / +30秒）。
- 分岐（もう1セット / 次へ）。
- リザルト: **総重量 と ポイント の2値** + 種目別内訳。
- **進行中セッションの逐次保存 + 起動時「続きから」**。
- **サブエージェント分担（並列）**:
  - A: ルーティン登録 UI + Dexie CRUD + オンボーディング（`store/`, `features/routine/`, `features/onboarding/`）
  - B: 実行画面ステートマシン + 画面遷移 + セッション永続化/復帰 + `game/`（`app/`, `features/workout/`, `game/`）※演出は stub 呼び出し
  - C: `effects/` 一式（PixiJS + Lottie + sound + 音声カウント、B の関数シグネチャに合わせる）
  - 統合と最終結線は本体。
- **受け入れ**: カメラ無しで1ルーティンを完走。フィーバーが発動し倍率が乗る。最終セット終盤で演出が強化。リザルトに総重量とポイントが正しく出る。リロード／バックグラウンド kill 後に「続きから」で復帰できる。
- **この時点で参照（GOD 系動画）とすり合わせ**、演出の方向性を確定 → 商用フリーアセットへの差し替え計画。

### Phase 2: 姿勢推定の導入

- MediaPipe `PoseLandmarker` を Web Worker + OffscreenCanvas で起動。`<video>` → Worker、33点受信の型付き I/F（`pose/PoseClient.ts`）。ミラー処理。
- 実行画面にスケルトン重畳（デバッグトグル）。
- **Cloudflare Tunnel + `vite dev`** で実機デバッグ。デバッグ HUD（fps / 推論ms / visibility / phase / セッション記録）実装。→ [デプロイ・実機検証計画.md](デプロイ・実機検証計画.md) §3.2・§9.1・§9.2・§10。
- スロットリング: 演出中は推論 fps を落とす。Page Visibility で停止。
- **サブエージェント**: 「MediaPipe Tasks Vision の Worker + OffscreenCanvas セットアップ最新手順」を調査系エージェントに委譲。実装は本体（中核・直列）。
- **受け入れ**: 実機で 15fps 以上を維持しつつ演出が破綻しない。バックグラウンドで推論停止・復帰。

### Phase 3: RepDetector（アームカール先行）+ キャリブレーション

- `detectors/angles.ts`（3点角度、EMA、信頼度ゲート、x 反転正規化）。
- `RepDetector` 基底（update / calibrateSample / finalizeCalibration）+ `armCurl.ts`。
- 実行画面のキャリブレーション状態を実装（3秒静止 + ゆっくり1rep → プロファイル保存 → 以後個人化閾値で判定）。
- `game/` と結線: 自動 rep → フィーバー抽選 → ポイント加算（フィーバー中 2x）。`confidence` 低下継続 → 警告 → セット完了画面で補正 UI。
- **フィクスチャ用クリップの収集**（ユーザー作業・物理）: アームカールをフル、他3種目はゴールデン数本。仕様は [デプロイ・実機検証計画.md](デプロイ・実機検証計画.md) §9.3.1。こちらでランドマーク JSON 化し `__tests__/fixtures/` にコミット。
- 精度計測: [デプロイ・実機検証計画.md](デプロイ・実機検証計画.md) §9.3 の定義・条件・手順。
- **サブエージェント**: なし（判定品質の肌感が要るため本体）。
- **受け入れ**: 通常テンポのカールで誤検出 < 5% / 取りこぼし < 10%（目標値、実測して閾値調整）。キャリブレーション前後で精度が改善する。

### Phase 4: 残り3種目へ横展開

- `shoulderPress` / `squat` / `sideRaise` を同じ基底 + キャリブレーションで実装。
- カメラ位置ガイド オーバーレイ（全身が枠内か、明るさ判定）。
- **サブエージェント分担（並列）**: 3種目を 3 エージェントに。各自 detector 実装 + リプレイ単体テスト（Phase 3 で収集したフィクスチャを使用。実機不要）。基底と `angles.ts` は Phase 3 で確定済み。
- 精度計測は Phase 3 と同じプロトコル（§9.3）を各種目に。
- **detector の汎用性を評価** → 種目追加の設計方針を決める（§12-4）。
- **受け入れ**: 4種目すべてで、自動 + 補正込みで1ルーティン完走。

### Phase 5: PWA 仕上げ・公開見据えた堅牢化

- Service Worker でモデル/アセットをプリキャッシュ。**初回ロードは長くて可**、以降のトレ体験が阻害されないことが条件。
- Wake Lock、画面回転（ポートレート固定）、カメラ権限拒否・切断・剥奪からのリカバリ UI。
- **バックグラウンド遷移 → 中断対応**（セッション復帰で担保）。着信は許容（拾う）。電池切れは対象外。
- 履歴一覧画面、過去リザルトの振り返り。
- エラーバウンダリ、AudioContext unlock、iOS standalone PWA でのカメラ動作確認。
- **公開準備（最小）**: 商用フリーアセットへの差し替え完了確認、他人の端末・部屋での初回体験の通し確認、OGP など最低限のメタ。ストア配信はしない。**アナリティクスは入れない（確定）**。
- **サブエージェント**: 履歴一覧画面を 1 エージェントに切り出し可能。
- PWA 検証項目・実機マトリクスは [デプロイ・実機検証計画.md](デプロイ・実機検証計画.md) §7・§8・§13。
- **受け入れ**: 機内モードで起動〜完走。権限拒否から復帰できる。実機2 OS で通し確認。

---

## 9. サブエージェント運用方針

- **並列化できる**: 独立したファイル群を新規に作るタスク。
  - Phase 1: A=登録UI / B=ステートマシン+game / C=effects（3並列）
  - Phase 4: detector 3種目（3並列）
  - Phase 5: 履歴一覧画面
- **直列でやる（本体）**: ステートマシン中核、Worker 統合、実行画面の結線、判定品質の作り込み（Phase 3）、`game/` の数値チューニング。
- **調査系はエージェントに委譲**: MediaPipe セットアップ、iOS Safari の getUserMedia/PWA 挙動など。結論とコード断片だけ回収。
- **渡す前に必ず I/F を固める**: 型定義（`pose/types.ts`、`RepDetector`、`game/` の I/F、effects の関数シグネチャ、Dexie スキーマ）を先にコミット。エージェントには「このファイルだけ / この型に従う / テストも書く」を明示。
- **統合は必ず本体**。

---

## 10. 横断的な非機能要件

- **発熱・電池**: 推論解像度を下げる / 演出中スロットル / Visibility で停止 / セッション時間の目安表示。
- **iOS Safari 固有**: standalone PWA でのカメラ、初回タップで AudioContext resume、Wake Lock 非対応時のフォールバック。
- **プライバシー**: 映像は端末内でのみ処理し外部送信しない旨を UI に明記。録画・保存なし。アナリティクス・クラッシュ収集なし。
- **アクセシビリティ**: 音 OFF でも成立する視覚フィードバック。タップ領域を大きく。

---

## 11. テスト戦略

- **detector ユニットテスト**: 記録した landmark 時系列（`__tests__/fixtures/`）をリプレイし、rep 数・phase 遷移・キャリブレーション前後の差を検証。カメラ不要、CI（GitHub Actions）で回す。
- **`game/` ユニットテスト**: rep イベント列を入力し、ポイント・フィーバー発動・倍率適用を検証。
- **ステートマシン**: 遷移テーブルのトランジションテスト（キャリブレーション / BONUS / もう1セット / 次へ / 中断復帰）。
- **手動 E2E チェックリスト**: 実機カメラでの通し確認（2 OS）。権限拒否・切断・機内モード・バックグラウンド復帰。詳細・端末マトリクス・挙動検証（fps・発熱・detector 精度）は [デプロイ・実機検証計画.md](デプロイ・実機検証計画.md)。

---

## 12. 未解決の設計判断（実装しながら詰める）

1. **フィーバーの具体設計**: 抽選確率、ゲージ充填量（固定 or rep 出来で変動）、抽選当選＝即発動か大チャンクか、フィーバー継続（rep 数 or 秒数）、倍率カーブ（2x 固定か段階か）。→ Phase 1 でプレイしながらチューニング。
2. **rep 判定の甘辛**: モチベ優先で「甘め」に倒す前提。具体閾値は Phase 3 の実測後に確定。
3. **「浅い rep」（`rangeOk=false`）の扱い**: カウントに含める / 演出だけ弱める / 除外。→ Phase 3 実測後。
4. **種目追加の設計**: detector の汎用化（設定駆動で角度・閾値を宣言）が現実的か。→ Phase 4 後に評価。
5. **ポイントの使い道**（アイテム / 敵・壁への攻撃 / 特典 / より強い敵）: MVP 対象外。ただし `totalPoints` は貯め続けてデータに残す。公開版の主要検討事項。

---

## 13. 設計判断ログ

### 2026-09-03 第1回

- 対象種目: 立位・正面4種に固定 / 自動カウント + タップfallback / PWA両OS / データはローカルのみ
- ~~総重量 = Σ(重量 × 完了セット数)~~ → 第2回で `Σ(rep × 実効重量)` に変更
- 重量 kg 固定 / 左右別カウント扱わない / 演出は既製品流用

### 2026-09-03 第2回

- **ポイント制を導入**: `Σ(rep × 実効重量 × フィーバー倍率)`。総重量（`Σ(rep × 実効重量)`、倍率非依存）とは別物。両方リザルト表示。将来ポイントを体験（アイテム / 敵・壁）に接続。
- **フィーバー**: 各 rep で抽選 + ゲージ満タンで発動。倍率 2x。GOD 系演出。数値は Phase 1 でチューニング。
- **終盤演出は最終セットのみ**。
- **自重種目**: 登録体重を実効重量に。オンボーディングで体重登録。
- **目標 rep 到達**: 自動終了しない。BONUS 表示 + 無操作で自動完了。
- **CV ロスト**: 推定継続 + 警告アイコン、セット完了画面で補正 UI。
- **キャリブレーション**: 初回・種目ごと 3 秒の可動域学習を `RepDetector` に組み込む。
- **中断復帰**: 進行中セッションを Dexie に逐次保存、起動時「続きから」。着信は許容、電池切れは対象外。
- **ゴール**: 最終目標は一般公開。まず個人ツールとして完成 → 公開版モック。
- **ノーススター**: パチスロ GOD 系（重厚フォント・重量感・シンプル・射幸心）。
- **種目**: 固定4 + 「その他（タップのみ）」。将来多種目対応。
- **音声 rep カウント**を入れる（アセットはユーザー調達）。
- **カメラ**: ミラー表示 + detector 内で正規化。
- **重量入力**: 0.5kg 刻み・下限 0。
- **CI**: GitHub Actions。
- **アナリティクス / クラッシュ収集**: 入れない（確定）。
- **アセット**: 商用利用フリーのみ。
- ピラミッドセット（1 Menu 内 重量可変）は実装しない。同種目・別重量は Menu を分ける。

---

## 14. 次アクション

- [x] 第2回設計判断を計画・CLAUDE.md に反映
- [ ] Phase 0 の scaffold に着手
- [ ] Phase 1 の型 I/F（Dexie スキーマ / ステートマシン遷移 / `game/` I/F / effects シグネチャ）を先に確定してコミット
- [ ] フィクスチャ用クリップの収集開始（ユーザー・アームカールのゴールデンから）
