# Cloudflare Workers Static Assets（旧 Pages）/ Netlify 形式のヘッダ規則。

/assets/*
  Cache-Control: public, max-age=31536000, immutable

/models/*
  Cache-Control: public, max-age=31536000, immutable

# WASM マルチスレッド化（SharedArrayBuffer）に切り替えるときだけ以下を有効化する。
# 有効にすると CORP/CORS 無しのクロスオリジン資源が読めなくなる点に注意（→ デプロイ・実機検証計画.md §6.2）。
# /*
#   Cross-Origin-Opener-Policy: same-origin
#   Cross-Origin-Embedder-Policy: require-corp
