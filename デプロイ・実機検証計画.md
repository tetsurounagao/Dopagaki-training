# デプロイ・実機検証計画

最終更新: 2026-09-03
親ドキュメント: [実装計画.md](実装計画.md)

このファイルは「スマホ実機でどう動作確認するか」「デプロイ環境をどう組むか」の詳細。
実装計画本体からはフェーズ単位でここへリンクしている。

---

## 目次

- [1. なぜ専用のデプロイ環境が要るか](#1-なぜ専用のデプロイ環境が要るか)
- [2. 環境の全体像（4層）](#2-環境の全体像4層)
- [3. 各環境の詳細](#3-各環境の詳細)
  - [3.1 ローカル（vite dev / preview）](#31-ローカルvite-dev--preview)
  - [3.2 Cloudflare Tunnel（実機デバッグ）](#32-cloudflare-tunnel実機デバッグ)
  - [3.3 デプロイプレビュー（Cloudflare Pages / Vercel）](#33-デプロイプレビューcloudflare-pages--vercel)
- [4. 推奨構成（結論）](#4-推奨構成結論)
- [5. ビルド／ホスティング設定](#5-ビルドホスティング設定)
- [6. HTTP ヘッダ設定](#6-http-ヘッダ設定)
- [7. PWA の検証ポイント](#7-pwa-の検証ポイント)
- [8. 実機検証マトリクス](#8-実機検証マトリクス)
- [9. 挙動検証（behavior verification）の具体](#9-挙動検証behavior-verificationの具体)
- [10. デバッグ HUD の仕様](#10-デバッグ-hud-の仕様)
- [11. ログ収集方針](#11-ログ収集方針)
- [12. フェーズとの対応](#12-フェーズとの対応)
- [13. チェックリスト（コピペ用）](#13-チェックリストコピペ用)
- [14. トラブルシューティング](#14-トラブルシューティング)

---

## 1. なぜ専用のデプロイ環境が要るか

- `getUserMedia`（カメラ）は **secure context 限定**。`localhost` は例外的に OK だが、スマホから見る LAN の生 IP（`http://192.168.x.x`）は secure context ではないので **iOS Safari はカメラを開けない**。
- Service Worker も同じく HTTPS（または localhost）限定。
- iOS のホーム画面追加 PWA（standalone）でカメラを使うには **正規証明書の HTTPS** が要る（自己署名だとインストールが汚く、挙動も不安定）。
- したがって実機確認には「スマホから正規 HTTPS で開けるURL」が必須 → トンネル or デプロイ。

---

## 2. 環境の全体像（4層）

| 層 | 何に使う | HTTPS | HMR | PWA/SW 検証 |
|---|---|---|---|---|
| ローカル `vite dev` | 開発 PC 上での実装 | localhost のみ | ○ | △（PC のみ） |
| ローカル `vite preview` | 本番ビルドの事前確認 | localhost のみ | × | ○（PC のみ） |
| Cloudflare Tunnel | **実機で HMR を効かせながらデバッグ**（閾値調整・fps 計測） | ○（正規） | ○ | ○ |
| Cloudflare Pages / Vercel | **ステージング・PWA挙動・共有・本番** | ○（正規） | × | ○（本番同等） |

---

## 3. 各環境の詳細

### 3.1 ローカル（vite dev / preview）

- `npm run dev` … PC ブラウザでの日常開発。`localhost` は secure context なのでカメラも SW も動く。
- `npm run build && npm run preview` … 本番ビルドを PC で確認。デプロイ前の最終チェックはまずここ。
- `@vitejs/plugin-basic-ssl` + `--host` で LAN に自己署名 HTTPS を立てる手もあるが、
  - iOS は証明書警告を手動許可する必要がある（「詳細を表示」→「このWebサイトにアクセスするR」）。
  - SW / PWA インストールは自己署名だと不安定。
  - → **実機は Tunnel を使う方針**とし、basic-ssl は緊急時の代替に留める。

### 3.2 Cloudflare Tunnel（実機デバッグ）

用途: RepDetector の閾値を実際に汗をかきながら詰める、fps・レイテンシ・発熱を実機で測る。HMR が効くのが利点。

セットアップ:

```bash
# 初回のみ（macOS）
brew install cloudflared

# 使うたび: vite dev を起動しておいてから
npm run dev            # 例: http://localhost:5173
cloudflared tunnel --url http://localhost:5173
# → https://<ランダム>.trycloudflare.com が発行される。スマホでこれを開く
```

注意点:

- quick tunnel の URL は **起動ごとに変わる**。origin が変わるので IndexedDB / SW キャッシュ / PWA インストール状態は毎回リセットされる。デバッグ用途なら許容。
- URL を固定したいなら named tunnel（CF アカウント + 独自ドメインが必要）。Phase 3 以降で計測を繰り返すなら検討。
- Vite の HMR を Tunnel 越しに通すため `server.hmr.clientPort = 443` を設定しておく（wss が 443 で張れないと HMR が繋がらない）。
- `server.allowedHosts` に `.trycloudflare.com` を追加（Vite が Host ヘッダを弾く場合）。

### 3.3 デプロイプレビュー（Cloudflare Pages / Vercel）

用途: PWA の本番挙動、オフライン起動、人に見せる、ブランチごとの差分確認。

- どちらもリポジトリ連携 → push で自動ビルド＆デプロイ。**ブランチ／PR ごとにプレビュー URL** が出る。
- 静的 SPA（バックエンドなし）なので無料枠で十分。
- 選定: DX なら Vercel がやや滑らか。無料枠の太さ・Tunnel と提供元を揃えるなら Cloudflare。**どちらでも可**。本計画は例を Cloudflare Pages 前提で書き、Vercel の対応も併記する。
- GitHub Pages は使わない（理由: カスタム HTTP ヘッダ不可 → 将来の COOP/COEP に詰む、プロジェクトパス配信で PWA の base 設定が面倒、PR プレビューなし）。

---

## 4. 推奨構成（結論）

| 目的 | 使うもの |
|---|---|
| 日常の実装 | `vite dev`（localhost） |
| 実機デバッグ（閾値調整・計測、HMR あり） | **Cloudflare Tunnel** → `vite dev` |
| ステージング・PWA検証・共有・本番 | **Cloudflare Pages**（または Vercel）にリポジトリ連携 |
| デプロイ前の最終確認 | `vite preview`（localhost） |

Phase 0 で Cloudflare Pages（or Vercel）連携まで済ませ、以降は `main` = 本番 / 各ブランチ = プレビュー で回す。

---

## 5. ビルド／ホスティング設定

Vite + vite-plugin-pwa 前提。

| 項目 | 値 |
|---|---|
| Build command | `npm run build` |
| Output directory | `dist` |
| Node version | 20 以上（`.nvmrc` か環境変数で固定） |
| SPA フォールバック | Cloudflare Pages: `public/_redirects` に `/* /index.html 200`（Vercel は Vite SPA を自動判定） |
| `base` | ルート配信（`/`）。プロジェクトパス配信はしない |
| MediaPipe 資産 | `.wasm` / `.task` は npm パッケージから `public/models/` にコピーして**自ホスト**（CDN 参照にしない。COEP と相性が悪いため） |

vite-plugin-pwa:

- `registerType: 'autoUpdate'`（SW を自動更新。取り残し事故を防ぐ）
- `workbox.globPatterns` にモデル/アセットを含め、オフライン起動できるようプリキャッシュ（サイズ上限に注意。`maximumFileSizeToCacheInBytes` を引き上げる）
- `manifest`: `display: 'standalone'`, `orientation: 'portrait'`, アイコン一式

---

## 6. HTTP ヘッダ設定

### 6.1 いま必要なもの

- **キャッシュ**: ハッシュ付きアセットとモデルファイルは長期 immutable。

Cloudflare Pages `public/_headers`:

```
/assets/*
  Cache-Control: public, max-age=31536000, immutable
/models/*
  Cache-Control: public, max-age=31536000, immutable
```

Vercel `vercel.json`:

```json
{
  "headers": [
    { "source": "/assets/(.*)", "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }] },
    { "source": "/models/(.*)", "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }] }
  ]
}
```

### 6.2 将来 WASM マルチスレッド化するときだけ追加

MediaPipe を multithreaded WASM（SharedArrayBuffer 使用）に切り替えて速度を稼ぐ場合のみ、cross-origin isolation が要る:

```
/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
```

- **デフォルトでは付けない**。COEP `require-corp` は CORP/CORS ヘッダの無いクロスオリジン資源（外部 CDN の Lottie・フォント等）の読み込みを止めるため。
- 付けるなら全アセットを自ホストし切ってから。Phase 2 の計測で「シングルスレッドで fps が足りない」と分かった場合の対応策として保持。

---

## 7. PWA の検証ポイント

| 項目 | 確認内容 | 主に見るフェーズ |
|---|---|---|
| manifest 認識 | インストールプロンプト／ホーム画面追加が出る | Phase 0, 5 |
| Service Worker 登録 | 初回登録・2回目以降のキャッシュヒット | Phase 5 |
| SW 自動更新 | デプロイ後にリロードで新版へ入れ替わる（取り残されない） | Phase 5 |
| オフライン起動 | 機内モードでアプリが起動し、ルーティン開始まで到達（モデルもキャッシュ済み） | Phase 5 |
| standalone 表示 | ホーム画面起動でアドレスバー無し・ポートレート固定 | Phase 2, 5 |
| standalone カメラ | ホーム画面起動の PWA でも前面カメラ映像が出る（iOS 16+） | Phase 2, 5 |
| 音声 unlock | 初回タップで AudioContext resume され、以降 SE が鳴る | Phase 1, 5 |
| Wake Lock | セット中に画面が消灯しない。非対応端末は無音ループ動画でフォールバック | Phase 5 |
| 権限リカバリ | カメラ拒否→設定案内、拒否状態でも手動モードで完走できる | Phase 5 |

---

## 8. 実機検証マトリクス

最低限そろえたい端末:

| 端末 | ブラウザ / 形態 | 優先度 |
|---|---|---|
| iPhone（最近の世代） | Safari | 必須 |
| iPhone（2〜3年前） | Safari | 必須（性能下限の確認） |
| iPhone | ホーム画面 PWA（standalone） | 必須 |
| Android（Pixel か Galaxy） | Chrome | 必須 |
| Android | ホーム画面 PWA | 必須 |
| iPad | Safari | 任意 |

各端末で見る列:

カメラ許可 / 前面カメラ映像 / 推論 fps / rep カウントの妥当性 / 演出の描画性能 / 音声 unlock / Wake Lock / 画面向きロック / オフライン起動 / インストール導線

---

## 9. 挙動検証（behavior verification）の具体

### 9.1 姿勢推定の性能

| 指標 | 測り方 | 目標（中位機） |
|---|---|---|
| 推論 fps | HUD で直近 10 秒のローリング平均 | 15 fps 以上を維持 |
| 1フレーム推論時間 | `performance.now()` 差分、Worker 内で計測 | 50ms 以下 |
| 表示 fps | `requestAnimationFrame` 間隔 | 演出中も 30fps 近辺 |
| rep→演出 発火遅延 | rep 判定時刻と演出開始時刻の差 | 100ms 以下（表示3フレーム程度） |
| ドロップフレーム | 取りこぼしたカメラフレーム数 / 総数 | 演出中に急増しないこと |

### 9.2 発熱・バッテリー

- 手順: 20 分相当のフルルーティンを連続実行（自動カウント + 演出 ON）。
- 判定:
  - 端末表面が「熱い」と感じるレベルにならない。
  - iOS のサーマルスロットル兆候（fps の階段状の低下）が出ない。
  - 開始時と終了時で推論 fps の低下が 30% 以内。
  - バッテリー消費の目安上限 ≈ 15% / 20 分（端末差が大きいので参考値）。
- 悪化時の対策: 推論解像度を下げる／推論を数フレームに1回に間引く／演出中は推論 fps を明示的に落とす／Page Visibility で完全停止。

### 9.3 RepDetector の精度

定義:

- **誤検出率** = 余計にカウントされた回数 ÷ 実際にやった回数
- **取りこぼし率** = 数え損ねた回数 ÷ 実際にやった回数

目標（通常テンポ・正面・適切な距離）:

- 誤検出率 < 5%
- 取りこぼし率 < 10%
- （実測して閾値を調整する前提の目標値）

計測条件（各種目でこの組み合わせを回す）:

| 軸 | 水準 |
|---|---|
| テンポ | ゆっくり / 通常 / 速い |
| 距離 | 1.5m / 2.5m / 3.5m |
| 明るさ | 明るい / 薄暗い |
| 服のコントラスト | 背景と明確に差がある / 紛らわしい |

手順:

1. HUD の「記録」で、動画（任意）＋ランドマーク列＋タイムスタンプ＋イベントを JSON ダンプ。
2. 人間が動画を見てグラウンドトゥルースの回数を数える。
3. ランドマーク列を detector にリプレイし、カウント結果を突き合わせて誤検出／取りこぼしを集計。
4. 記録した JSON は `src/detectors/__tests__/` のリプレイ資産として残す（以後 CI で回帰確認）。

### 9.4 フロー・演出まわり

- 「もう1セット」分岐: 暗転→フラッシュ→大型テキスト→カウントダウンの遷移が詰まらない。
- 最終セット終盤（残り 5〜10 回）で演出強度が上がる閾値の見え方。
- インターバルのカウントダウンとセット再開のタイミング。
- 音 OFF でも視覚だけで rep 成立が分かる。

---

## 10. デバッグ HUD の仕様

- 有効化: クエリ `?debug=1`、または画面隅トリプルタップ。
- 表示項目: 表示 fps / 推論 fps / 推論 ms / ランドマーク最小 visibility / 対象関節角度 / detector の phase / rep カウント / confidence / ドロップフレーム数。
- ビデオ上にスケルトン重畳（オン・オフ）。
- 「セッション記録」ボタン: ランドマーク列＋タイムスタンプ＋イベント（rep, phase 遷移, 演出発火）を1つの JSON にまとめてダウンロード。**ネットワーク送信なし**。
- 実装場所: `src/features/workout/DebugHud.tsx` 想定。本番ビルドでもクエリで出せるが、通常UIには露出しない。

---

## 11. ログ収集方針

- 計測ログ・ランドマーク記録は **端末内に留める**（IndexedDB もしくは手動ダウンロード）。外部送信・解析サービスは使わない。
- 映像は保存しない（記録機能で動画を残す場合も端末ローカルのみ、明示操作時だけ）。
- プライバシー方針（映像は端末内処理のみ・非送信・非保存）を UI に明記する。

---

## 12. フェーズとの対応

| フェーズ | このドキュメントで関係する作業 |
|---|---|
| Phase 0 | Cloudflare Pages（or Vercel）にリポジトリ連携。`dist` 出力・SPA フォールバック・`base=/`・Node 20 固定。1 端末でインストール導線をスモークテスト。→ [5](#5-ビルドホスティング設定) [6.1](#61-いま必要なもの) |
| Phase 1 | ブランチ・プレビュー URL を、並列サブエージェント3人の統合確認に使う（カメラ不要）。演出・フロー・リザルトを実機ブラウザで通す。 |
| Phase 2 | Cloudflare Tunnel + `vite dev` で実機デバッグ。HUD 実装。実機マトリクスで fps・推論ms・発熱を計測。→ [3.2](#32-cloudflare-tunnel実機デバッグ) [9.1](#91-姿勢推定の性能) [9.2](#92-発熱バッテリー) [10](#10-デバッグ-hud-の仕様) |
| Phase 3 | HUD の記録機能で armCurl のランドマーク列を条件別に収集し、[9.3](#93-repdetector-の精度) の手順で精度計測。リプレイ資産化。 |
| Phase 4 | 残り3種目に同じ精度プロトコル。サブエージェントは実機ではなくリプレイ資産で単体テストを回す。 |
| Phase 5 | PWA 全項目の検証（[7](#7-pwa-の検証ポイント)）。オフライン起動・Wake Lock・権限リカバリ・standalone カメラ。実機マトリクス（[8](#8-実機検証マトリクス)）でサインオフ。 |

---

## 13. チェックリスト（コピペ用）

### Phase 0 デプロイ基盤

- [ ] リポジトリを Cloudflare Pages（or Vercel）に連携
- [ ] Build command `npm run build` / Output `dist` / Node 20
- [ ] SPA フォールバック（`_redirects` or 自動）
- [ ] `base` がルート（`/`）
- [ ] `_headers`（or `vercel.json`）でアセット/モデルの長期キャッシュ
- [ ] `main` push → 本番 URL 発行を確認
- [ ] ブランチ push → プレビュー URL 発行を確認
- [ ] 実機1台でホーム画面追加まで確認

### Phase 2 実機デバッグ環境

- [ ] `cloudflared` インストール
- [ ] `server.hmr.clientPort = 443` / `allowedHosts` に `.trycloudflare.com`
- [ ] Tunnel 経由でスマホから開けて HMR も効く
- [ ] `?debug=1` で HUD 表示
- [ ] HUD にセッション記録（JSON ダウンロード）実装

### 実機マトリクス（各端末で）

- [ ] カメラ許可ダイアログ → 前面カメラ映像
- [ ] 推論 fps 15 以上
- [ ] rep カウントが目視と概ね一致
- [ ] 演出中も表示がカクつかない
- [ ] 初回タップで SE が鳴る
- [ ] セット中に画面が消えない
- [ ] ポートレート固定
- [ ] 機内モードで起動（Phase 5）
- [ ] ホーム画面 PWA でカメラが出る

---

## 14. トラブルシューティング

| 症状 | 原因 / 対処 |
|---|---|
| iOS でカメラが起動しない | secure context か（正規 HTTPS or localhost）。`getUserMedia` はユーザー操作起点で呼ぶ。`<video>` に `playsinline` `autoplay` `muted`。設定 > Safari > カメラ を確認 |
| iOS ホーム画面 PWA でカメラが黒い | iOS 16+ 必須。`playsinline` + `muted` + `autoplay` が揃っているか。ストリーム取得後に `video.play()` を明示 |
| SW が新版に入れ替わらない | vite-plugin-pwa を `registerType: 'autoUpdate'`。必要なら `skipWaiting`/`clientsClaim`。強制リロードで確認。モデルファイルの古いキャッシュにも注意 |
| 自己署名証明書で iOS が繋がらない | 証明書警告で「詳細を表示」→「このWebサイトにアクセス」。それでも SW が不安定なら Tunnel に切り替え |
| COEP 有効化後に一部アセットが読めない | クロスオリジン資源が CORP/CORS を返していない。全アセットを自ホストするか `crossorigin` 属性を付ける。不要なら COEP を外す |
| trycloudflare の URL が毎回変わる | quick tunnel の仕様。固定したいなら named tunnel（CF アカウント + ドメイン） |
| Tunnel 経由で HMR が繋がらない | `server.hmr.clientPort = 443`、`server.hmr.protocol = 'wss'` を設定 |
| Vite が「Blocked request. This host is not allowed」 | `server.allowedHosts` に `.trycloudflare.com`（や CF Pages のドメイン）を追加 |
| 演出を出すと推論 fps が急落 | 推論を Worker + OffscreenCanvas に分離。演出中は推論を間引き。解像度を下げる |
